# Core must not depend on the calling context (the UI thread's SynchronizationContext). Enforcing
# ConfigureAwait(false) guarantees the loop runs on the thread pool, which is what keeps the app-exit held-Up
# guarantee (the synchronous wait in the UnhandledException handler) from deadlocking.
[*.cs]
dotnet_diagnostic.CA2007.severity = error
